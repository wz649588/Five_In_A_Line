<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fiveinarow">

    <application
        android:allowBackup="true"
        android:icon="@drawable/stone_b1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NetBattle" />
        <activity android:name=".LoadSheetActivity" />
        <activity android:name=".GetSheetFiles" />
        <activity android:name=".NetBattlePalyers" />

        <service
            android:name=".OnClearFromRecentService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false" />
        <service
            android:name=".NetBattleService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>